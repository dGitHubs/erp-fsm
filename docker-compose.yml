version: "3.9"

services:
  db:
    image: postgres:16
    container_name: erp_fsm_db
    environment:
      POSTGRES_DB: erp_fsm
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
